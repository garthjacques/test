on:
  workflow_call:
    inputs:
      username:
        required: true
        type: string
  workflow_dispatch:
    inputs:
      username: hello
      type: string

jobs:
  example_job:
    name: ${{ inputs.name }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
